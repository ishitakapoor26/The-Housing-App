import 'package:flutter/material.dart';

class Leadregistration extends StatefulWidget {
  const Leadregistration({Key? key}) : super(key: key);

  @override
  State<Leadregistration> createState() => _LeadregistrationState();
}

class _LeadregistrationState extends State<Leadregistration> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('lead registration'),
      ),
    );
  }
}
